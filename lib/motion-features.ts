// Loaded asynchronously by LazyMotion so animation features stay out of the first-load bundle.
// domMax is required for layout / layoutId animations (nav indicator, filters, tabs).
import { domMax } from 'framer-motion'

export default domMax
